# Build output. Everything here is made by tools/build.sh and tools/release.sh.
build/
out/
*.jar
*.class

# Archives produced by tools/release.sh
FractalJDE-*.zip

# Logs and scratch from a checking run
*.log
selftest.log
selftest.txt

# The base system image, which is built rather than kept
BaseSystem.dmg

# What cargo leaves behind building the launcher. src/mark.mask is kept, because it is
# what lets the launcher build on a machine with a Rust compiler and no Java.
tools/launcher/target/
